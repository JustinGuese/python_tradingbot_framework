# Cluster overrides. Committed, non-secret. Fed into the ConfigMap by `ske apply`.
# The Helm chart takes its non-secret settings from helm/tradingbots/values.yaml
# (explicit `env:` beats envFrom), so this only carries what the pods do not
# already get from there.
APP_ENV=cloud
